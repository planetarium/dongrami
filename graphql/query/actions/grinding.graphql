query grinding(
  $avatarAddress: Address!
  $equipmentIds: [Guid]!
  $chargeAp: Boolean
) {
  actionQuery {
    grinding(
      avatarAddress: $avatarAddress
      equipmentIds: $equipmentIds
      chargeAp: $chargeAp
    )
  }
}
